/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js,jsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
// This configuration file sets up Tailwind CSS for a React project.
// It specifies the content files to scan for class names, extends the default theme, and includes
